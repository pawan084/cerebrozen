# Android / Gradle
*.iml
.gradle/
build/
local.properties
.idea/
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab
# Upload signing key. Losing it means the app can never be updated on Play;
# committing it means anyone can ship as us. Both extensions keytool emits.
*.keystore
*.jks
.kotlin/
# Firebase / Google config — contains API keys, never commit.
google-services.json
**/google-services.json
