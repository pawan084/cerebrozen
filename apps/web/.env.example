# Backend API base URL (the FastAPI server). Used by the waitlist form.
NEXT_PUBLIC_API_URL=http://localhost:8000

# App Store listing URL. Leave blank until launch — the download badge then
# points to the waitlist. Set to the real https://apps.apple.com/... URL at launch.
NEXT_PUBLIC_APP_STORE_URL=
