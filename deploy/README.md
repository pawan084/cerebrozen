# deploy — Production Deployment

Not built yet (Phase 4, see `docs/TODO.md`). Caddy reverse proxy
(auto-TLS, security headers) mapping cerebrozen.in / app. / admin. / api.
subdomains; hardened `docker-compose.prod.yml` with expose-only services.
Patterned on `ref/Zen/deploy`.
