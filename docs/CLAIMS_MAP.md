# CereBro — Claims Map

> Every user-facing claim that could be doubted, and the mechanism that makes it
> true. If a claim has no mechanism, it does not ship.
>
> The automated half is `scripts/check-claims.mjs`, which fails the build on a
> banned phrase across **all four clients** — web, app, iOS Swift and Android
> `strings.xml`. Copy honesty was previously enforced by review alone, and this
> session found three live over-claims plus a fabricated screen, which is why
> the gate exists again. Companions: [PRD.md](PRD.md) (honest per-feature
> status), [ARCHITECTURE.md](ARCHITECTURE.md).

## How to use this file

- **Adding a claim?** Add a row with the mechanism *and* the test that pins it.
  A row without a test is an intention, not a claim.
- **Gate flagged you?** Fix the copy, or — if the capability genuinely now
  exists — add the row here and remove the phrase from the script. Do not add a
  silent allowlist; that turns the gate into decoration.
- **Removing a feature?** Delete its row and re-ban the phrase.

## 1. Privacy claims

| Claim (as shown) | Mechanism | Test |
| --- | --- | --- |
| "No ads, ever · no third-party trackers" | Zero ad/analytics SDKs in any client; first-party `/events` with an allow-list | `tests/test_events.py` |
| "Support tooling shows counts and account state, not your words" — extended to the crash reporter | `services/errors.py` builds an error report by **allow-list**, never by scrubbing a rich context: the exception TYPE but not its message (asyncpg and pydantic quote the offending value, which on this product is a person's sentence), stack frames as positions but never their locals, the route TEMPLATE rather than the path (an id in a URL is not an identifier in a report), and the user only as a 12-char HMAC so "how many people hit this" is answerable without anyone being named. No body, query, header or cookie is copied at all | `tests/test_error_tracking.py` (26), `tests/app/errors.test.ts` (27), `ErrorTrackingTest.kt` (15) — one policy ported to all three, most cases asserting ABSENCE against the whole serialised payload rather than the field a leak was expected in |
| "Reporting uses anonymous group totals, minimum cohort thresholds and small-cell suppression. Managers cannot see who used CereBro" | The threshold is defence in depth over a number that is already non-behavioural. Every figure an organisation can read counts MEMBERSHIP ROWS whose status the organisation itself set — `add_member` and the CSV import write `active`, `end_membership` writes `ended`, and NOTHING transitions a membership because of anything the member did. The reporting surface never reads `MoodLog`, `JournalEntry`, `ChatMessage` or `SleepLog` at all, so differencing the aggregates reveals only what the differencer already did | `tests/test_org_reidentification.py` — runs the differencing attack end to end, proves a person cannot be counted in two cohorts (409), and asserts two organisations report IDENTICALLY when one's members use the product constantly and the other's have never opened it |
| "Private reflection with gentle prompts, and a search that only you can run" — and the same for every other row a person owns | Every route that takes a row id compares that row's `user_id` to the caller and answers **404, never 403** — a 403 would confirm the row exists and turn each refusal into an oracle for enumerating other people's ids. Two shapes are in use: a post-hoc `if row is None or row.user_id != user.id`, and scoping inside the WHERE clause, which is the version that cannot be spotted by reading the `if` alone. Ownership is checked at the row that actually carries it — a plan step is owned through its plan, so the check hops step to plan to user | `tests/test_cross_user_access.py` — a table of ~18 routes, each called by a second account holding a real id, asserting the 404, that the response never echoes the victim's content, that the victim's row is UNCHANGED when read back (a 404 that wrote anyway is worse than a 200), and that a real id and an invented one are byte-identical. Pinned by mutants T1–T6: deleting any one ownership check fails a test |
| "Confirm your email to use this" (403 on provider-backed features) | `services/verification.py` — applies only to an account that is unverified AND unpaid AND on a deployment that can actually send email, since demanding a proof the product cannot deliver is an outage wearing the clothes of a policy. Chat is never gated: an unverified account gets a smaller daily allowance, not a locked door | `tests/test_email_verification_gate.py` — including the class asserting the account, journal, export and safety-plan surfaces stay open, so an unconfirmed address never becomes a hostage over somebody's own words |
| "Usage counts are anonymous and optional" | Random install id, never account-linked; opt-out toggle | `tests/test_events.py` |
| "Support tooling shows counts and account state, not your words. One exception: if an entry is flagged as a crisis, a reviewer can open that entry — and every time one is opened, that is recorded" | Admin views project counts only; `admin.read_safety_excerpt` is the single path to verbatim text, is per-row and deliberate, and writes an `admin_audit` row naming the admin. Android said "never the words" until 2026-08-12 — an absolute this endpoint disproves. The path is defensible; denying it was not | `tests/test_admin_metrics.py` (incl. `context_memories` coverage), `tests/test_admin_audit_log.py` |
| "Export or delete everything from inside the app" | `GET /users/me/export` enumerates every user-scoped table; `DELETE /users/me` cascades | `tests/test_account.py`, `tests/test_memory.py`, `tests/test_safety_plan.py`; **and from the client** (2026-08-21) — `PrivacyFlowE2ETest` asserts the export carries real mood rows rather than an empty envelope, and `AccountDeletionE2ETest` signs up a throwaway account, writes to it, deletes it, and proves the credentials stop working |
| "Turn memory off and it forgets" | `ai_memory` gates reads/writes of `context_memories`; deletion is never gated | `tests/test_memory.py::test_consent_off_blocks_reads_and_writes_but_never_deletion`; **and the switch itself** (2026-08-21) — `ConsentFlowE2ETest` drives the real `AppSwitch` on the privacy screen and then asks the server what it believes, in both directions, then proves a write is refused while a delete still succeeds. Verified by mutation: wiring the toggle to nothing fails it with "the switch read On but the server did not record the grant" |
| "This note stays with the check-in… the companion is given your feeling — never these words" (`/checkin`) | `agentic._recent_signals` selects `MoodLog.mood`, not the row — so the note column never reaches the plan generator, while the feeling itself does (consent-gated on `mood_history`) | `tests/test_dpdp.py::test_the_companion_is_given_the_feeling_never_the_note` |
| "Here is exactly which of them are switched on for you right now" (`/checkin`) | The read list is rendered from a live `GET /users/me/consent`, per category, rather than stated flatly; a failed read says so instead of guessing. The design mock's flat "Does not use your journal" was true only while `journal_memory` was off | `tests/test_dpdp.py::test_plan_signals_respect_itemized_consent`, `::test_a_fresh_account_has_granted_nothing` |
| "Delete all memory" | Wipe clears chat, insights, saved notes and the Oracle checkpoint | `tests/test_memory.py::test_wipe_all_removes_memories_too` |

## 2. Safety claims

| Claim (as shown) | Mechanism | Test |
| --- | --- | --- |
| "Crisis-aware · region-correct lines always a tap away" | `services/crisis.py` per-region resources appended to every flagged reply | `tests/test_crisis.py` |
| "Tele-MANAS 14416 first in India" | Region ordering in `crisis.reply_suffix("IN")` | `tests/test_crisis.py` |
| "Safety never blocks" | Crisis scanning adds resources; it never rejects a message, and a safety plan never gates a reply. **Nor does the billing path** (2026-08-23): `usage.enforce_quota` used to run before anything read the text, so a free user at message 51 met a 429 and an upgrade prompt with no scan, no safety event and no escalation — the claim was true of `services/safety.py` and false of the request. `routes/chat.py` now consults `safety.keyword_floor` first (local, free, no model call, so it cannot be used to burn tokens) and waives the cap for anything it flags | `tests/test_safety_plan.py::test_crisis_reply_is_unchanged_with_and_without_a_plan`; `tests/test_email_verification_gate.py::TestChatIsNeverLockedShut` drives it over the cap end to end, and mutants V1–V2 fail if either half of the waiver is removed |
| "Your safety plan is yours — we don't score or share it" | No read path outside the owner; no model write path exists at all | `tests/test_safety_plan.py::test_one_account_cannot_see_anothers_plan` |
| "Not a therapist, diagnosis, or crisis service" | Disclosure copy on every AI surface; no diagnostic or scoring code exists | `DisclosureCopyTest` (Android), PRD §2 |
| **"Verified"** (crisis screen badge) | Only rendered where the numbers were actually checked against a named source. `VERIFIED_CRISIS_REGIONS` is `{IN}` — MoHFW Tele-MANAS and the ERSS 112 listing, the same two sources `/safety` cites. Every other region reads "Not verified yet" and says why. The claim appears three times on that screen (badge, strapline, and the detail line under the number someone would dial); all three branch on the same predicate, so they cannot disagree | `CrisisVerifiedBadgeTest` (Android) |
| "We haven't checked these against an official source" (non-IN regions) | The honest branch of the above — shown instead of the badge, not in addition to it | `CrisisVerifiedBadgeTest::an unknown region is not verified` |
| "Someone you selected; reached automatically **only if you switched that on**" | `escalation.on_crisis` notifies the trusted contact on a crisis-level `SafetyEvent`, hard-gated on `TrustedContact.notify_consent` (default `False`). Until 2026-08-12 this line read "CereBro never contacts them automatically" — an absolute the backend disproves the moment consent is on, and one the trusted-contact screen already contradicted two taps away | `SafetyCopyTest::the trusted-contact line does not deny an automatic contact that happens`, `tests/test_escalation.py`; and `tests/test_escalation_truth.py` for the half that was missing — "reached automatically" is only true when somebody was actually reached. Both senders swallow their own failures by design (a Twilio rejection must not 500 the message that triggered the scan), and `escalated` was set regardless, so a contact who was never told was recorded exactly like one who was. It now tracks the real outcome, and `escalation_note` carries WHY nobody was reached — withheld consent is the product working, a failed send is an incident, and a reviewer must be able to tell them apart |
| TIPP's self-harm note — a risk signal that carries its own pathway | The note *is* the crisis door (`onUrgent` → `crisis`), not a description of where one lives. It previously ended "Urgent support lives in the You tab" on a screen whose only controls were Back/Previous/Next | `SafetyCopyTest::the TIPP self-harm note does not send the user hunting for a tab`; pathway walked on hardware 2026-08-12 |

## 3. Capability claims

| Claim (as shown) | Mechanism | Test |
| --- | --- | --- |
| "Unlimited daily conversations · free includes 50 messages a day" | `services/usage.py` — real DB count, 429 past the cap, premium tiers exempt | `tests/test_usage_limit.py` |
| "Patterns only appear once real check-ins support them — no guesses" | `compute_patterns` thresholds every rule and returns `[]` below them | `tests/test_patterns.py` |
| Landing pricing — "Narrated audio for the sleep and calm sessions that have it" (Premium) | `services/media.playback_url` returns `""` for an un-entitled premium item, so the file is never handed out; the client falls back to its ambient bed. This and the message cap are the **only** two things the backend gates on tier — the pricing table is two lines long for that reason | `tests/test_entitlements.py::test_premium_narration_follows_the_resolved_tier` |
| Landing pricing — "A day missed is forgiven, never counted against you" | `metrics.user_streak` forgives one missed day inside a run, and no surface renders a "miss" count. Previously read "a missed day dims, **it never resets**"; the streak *does* start over after two missed days, and the browser app renders that very count — the forgiveness was real, the absolute was not | `tests/test_admin_metrics.py::test_streak_endpoint_mirrors_ios_rules` — asserts the single grace day and that the run ends at the next gap |
| "Edit or delete any of it" | `context_memories` is addressable: PATCH/DELETE per row | `tests/test_memory.py`; `PrivacyFlowE2ETest::a_memory_can_be_added_edited_and_deleted_one_at_a_time` adds, edits and deletes one from the device and asserts it is gone (2026-08-21) |
| "Works in your browser" | `apps/app` is a real authenticated client with public signup | `e2e/tests/app.spec.ts` |
| "Private by design — no ads, nothing sold, and nothing remembered unless you allow it" | Three separate mechanisms: zero ad/third-party SDKs in any client; no data sale (first-party `/events` only); and all six consent categories default **off**, with reads/writes gated on them | `tests/test_events.py`, `tests/test_consent_enforced.py`, `tests/test_dpdp.py::test_a_fresh_account_has_granted_nothing` |
| "Safety scanning … never blocks your writing, and nobody at CereBro reads it" | `services/safety.py::scan_and_record` only ever RAISES a risk level and attaches resources — no code path rejects or edits an entry; admin surfaces project counts, and an excerpt read is a separate, logged, per-row GET | `tests/test_safety_reach.py`, `tests/test_admin_metrics.py` |
| "My safety plan — yours, in your words · works offline" | `Session.api` caches every GET and serves the last copy when a read fails; the screen shows the cached plan with an honest "saved on this device" banner, and says so plainly when there is no cached copy rather than showing empty boxes | `SafetyPlanTest`, `tests/test_safety_plan.py`; the three network states verified on hardware 2026-07-31 |
| "First-party — computed on your own data, never sold or shared" (Weekly insights) | `services/insights.py` imports no AI module at all — the weekly read is pure SQL over the user's own rows, and each category is gated on its own consent flag, so a switched-off category reads as "no data" rather than being computed anyway | `tests/test_insights_no_guesses.py`, `tests/test_insights_no_guesses.py` |
| "Two-minute reset" / "Try a 2-minute reset" / "Fast anxiety-stress reset — 2 minutes" | `twoMinutesReached` in `ui/screens/Breathe.kt` derives elapsed seconds from completed cycles at the user's chosen pace and marks the two-minute point once it has genuinely passed. The exercise stays open-ended — it is a mark, not a timer or a stop | `BreathePacingTest` (mark timing, slower paces, Reset-only); verified in real time on hardware 2026-07-31 |
| **"At most one nudge a day, and nothing ever resets"** (Android Reminders, 2026-08-16) | `notify/Reminders.shouldPost` refuses a second post on a date already stamped in `nudge_last_posted`, so a re-armed alarm, a reboot and a second dispatcher cannot stack nudges. Nothing in the notify path counts misses or decays | `RemindersTest::the_alarm_posts_at_most_one_nudge_a_day`, `::shouldPost_enforces_both_rules_including_the_midnight_wrap` |
| **"No nudges in this window" / "Set both ends to the same hour to stay quiet all day"** (quiet hours) | Same pure function: the window wraps midnight (22→07 default), and `start == end` means quiet for the whole day, which is how someone switches nudges off without hunting for a toggle. A suppressed nudge is **dropped, not queued** — a check-in prompt arriving at 07:00 about yesterday is noise, and the inbox already keeps what was missed | `RemindersTest::quiet_hours_drop_the_nudge_entirely`, `::shouldPost_enforces_both_rules_including_the_midnight_wrap` |
| **"Tap how you feel … the app never has to open"** (notification quick action) | The "Check in" action targets `notify/QuickLogActivity`, a translucent dialog that writes through the same `/moods` API as every other surface and finishes itself; it falls back to opening the app only when the save cannot succeed. It deliberately does **not** render over the lock screen — a mental-health prompt on a locked phone is a privacy leak in family contexts | `RemindersTest::the_notification_carries_the_quick_log_action`; manifest has no `showWhenLocked` |
| **"%d check-ins in the last %d days — counted, never scored"** (Trends → Which feelings showed up) | `moodCounts` counts occurrences per wire mood and orders by frequency. It assigns no value to any feeling: the deliberate decision (docs/TODO.md, V3 deferred items) was to **refuse** a mood sparkline, because giving each mood a height claims "Anxious" sits below "Tired". The 1–5 ease line above it is the server's own measure, gap-broken and gated on `enough_data` | `CompanionFirstTest::moods are counted, never scored`, `::a check-in with no mood or a broken stamp is skipped, not bucketed` |
| ~~"You showed up on %d days this month"~~ (You → Your month) | **Retired 2026-08-16 by the V6 density pass** — the grid and its copy left the You screen, so the claim is no longer made anywhere. `presenceMonth` and its test stay: the presence framing (two check-ins on one day light one dot; no streak, no percentage, no reset) is the rule any future surface must meet | `CompanionFirstTest::presence month counts days shown up and nothing else` |
| **"picked with you, not for you"** (Home → Today's care) | Rendered only for a plan the SERVER built from this account's own signals (`showsCarePlanProvenance` → `PLAN_STEP`/`PLAN_DONE`). The offline/no-plan fallback is the identical three-minute grounding practice for everyone and names its own reason; the line used to sit outside the branch, so the fallback printed a personalization claim nothing supported | `ScreenLogicTest::only_a_real_plan_claims_it_was_picked_with_you` |
| **The journal mood pill** (an entry showing "Calm", "Anxious", …) | The pill renders only the feeling **the writer chose** in the composer, stored as a `mood:<wire>` tag on the entry. It is never inferred from the entry's words and never borrowed from that day's check-in; an unknown or hand-written tag renders no pill rather than raw text | `CompanionFirstTest::a journal mood pill shows only what the writer chose` |
| **"noted — that's last night in your diary"** (chat sleep opener) | The opener's sleep chips call the same `Api.logSleep` the Sleep form calls; a guest gets the honest "I can't save a night without an account" instead, and a failed write says so and points at the Sleep tab | `ApiEndpointsTest` (the `/sleep` POST contract); walked on device 2026-08-16 |
| **"One thing done today. That counts."** (Home hero) | `stepsDoneToday` counts only steps whose **`done_at` falls on today** — `done` alone would keep saying this on a Friday about something ticked on Monday. A step with no usable timestamp is not counted rather than assumed to be today's. The field reaching the client is itself new: `PlanStepOut.done_at` was written by the toggle route but never serialized until 2026-08-16 | `CompanionFirstTest::a step ticked last week is not today's win`, `tests/test_data_flows.py::test_a_step_reports_when_it_was_done_not_just_that_it_was` |
| **"You checked in a little while ago — today's held."** / **"It's been a few quiet days — nothing to catch up on."** (Home hero) | `heroLineFor` picks the line from real state in a fixed order (offline → check-in <90 min → step done today → ≥3 quiet days → the week → empty). Presence framing: every branch names something that *happened*. A first run has no check-in at all, so it gets the empty line — it is never told it has been "quiet" | `CompanionFirstTest::the hero says what you most recently did, not a weekly average`, `::quiet days are named kindly, and a blank first run is not called quiet`, `::offline always wins, because it is the only line that changes what works` |
| **"you tried that a moment ago — how did it land?"** (proactive follow-up) | `followUpOwed` fires only on real events: a suggested activity actually opened from chat, or a ≥3h gap since the last message. An empty thread stays the opener's job, and an unparseable timestamp keeps the companion quiet rather than guessing — it is never a timer that greets you for glancing at the tab | `CompanionFirstTest::an activity you opened is asked about when you come back`, `::a long gap earns a welcome back, a short one earns silence`, `::an empty thread is the opener's job, not the follow-up's` |

| **"Saved on this device — it will sync, and count towards your streak, once you are back online"** (web check-in, journal, sleep, 2026-08-20) | `apps/app/lib/outbox.ts` — a localStorage write queue with the idempotency key minted at QUEUE time (not send time, or a replay after a crash creates a second check-in), drained oldest-first with one failure stopping the drain. A 4xx is **rethrown, never queued**: the request is wrong, and "saved, will sync" would be a lie found out later. Only 408/429/5xx and a fetch that never answered are kept. Mirrors `net/Outbox.kt`, against `backend/app/services/idempotency.py` | `e2e/tests/app.spec.ts::a check-in made offline is kept, then sent when the network returns` — goes offline mid-page, asserts the queued wording rather than "Saved", then asserts the streak the SERVER computes actually moves |
| **"Your entry is saved on this device… the safety check that runs on new entries has not read it"** (web journal, queued) | The risk scan is server-side (`services/safety.py`), so a queued entry genuinely has not been scanned. `journal/page.tsx` leaves `support` untouched when `send` returns null instead of rendering the no-support state, which would have read as "checked, nothing found" | Same spec; the branch is the `entry === null` path in `save()` |
| **"the sound is generated here, in this tab"** (web soundscape mixer, 2026-08-20) | `apps/app/lib/mixer.ts` synthesises all four layers with the Web Audio API (filtered white/brown noise; detuned oscillators for the drone) — there is no audio file to fetch, and every `ambience.*` row in the catalogue currently ships an empty `url`. An uploaded server asset supersedes a synthesised layer per layer, the same precedence `SoundscapeMixer` documents, and the label under each slider says which of the two you are hearing | `apps/app` typecheck + the label is derived from the catalogue response, not hardcoded; assets verified empty against `/media/catalog?kind=ambience` on 2026-08-20 |
| **"the clip is turned into words you can read and edit before anything is sent"** (web voice, 2026-08-20) | `lib/voice.ts` posts the recording to `/voice/stt` and puts the transcript in the COMPOSER; nothing is sent to the companion without a second, deliberate press. The microphone track is stopped explicitly on the way out, so the browser's recording indicator goes off with it. The button only exists when `/voice/status` says the key is configured **and** the browser can record — no dead control, the same ruling Android applies to its Google button | `tests/test_voice.py` (the endpoints and the `voice_storage` enforcement); the gate is `voice?.stt` in `chat/page.tsx` |
| **"Log at least three nights before CereBro describes a direction"** (web Sleep insights, 2026-08-20) | Every derived number is gated on the server's `enough_data`, which is false under `MIN_NIGHTS`; under it the API returns zeros, and printing "0h 0m" would read as a measurement rather than an absence | `tests/test_sleep.py` (the summary contract); the page renders `—` for each tile whenever `enough_data` is false |
| **"Days you did not log are missing rather than drawn as zero"** (web Trends, 2026-08-20) | `services/trends.py` omits days with no data from the series, and the mood↔sleep correlation is withheld with a machine-readable `reason` until `MIN_CORRELATION_PAIRS` overlapping days exist — two points always correlate perfectly, which is why two points are never drawn as a finding. The page renders the reason, never a number | `tests/test_trends.py` |
## 4. Claims the public pages make (2026-08-12)

Three pages from `ref/landing.html` shipped this session. Each one states
something the product is held to **and** what is not true yet — and the second
half is the half that rots quietly, so the e2e walks both.

| Claim (as shown) | Mechanism | Test |
| --- | --- | --- |
| `/safety` — "Human help comes first" with 14416 and 112 on the page | Not a link to a link: both numbers render in the page body, so the pathway survives a failed client-side nav | `trust-pages.spec.ts::safety centre leads with human help and names the limits` |
| `/safety` — "What is not in place yet" | A standing section, not a footnote. Present tense on purpose: it lists the gaps rather than implying a roadmap | same test (the heading is asserted) |
| `/accessibility` — "we do not claim conformance" | **A claim about the absence of a claim.** No WCAG level is asserted anywhere on the site; `check-contrast.mjs` gates measured text contrast, which is one criterion, not conformance | `trust-pages.spec.ts::accessibility page claims no conformance it has not earned` |
| `/organizations` — "not yet available" | There is no org checkout, no seat billing and no B2B plan in `check-prices.mjs`'s price set. The page describes the model without offering it | `trust-pages.spec.ts::organizations page states the boundary and that it is not yet buyable` |
| `/organizations` — the list of what a sponsor "must never receive" | The same boundary the admin API enforces: `apps/portal` and the admin metrics endpoints project counts only; content is a separate, logged, per-row GET that no org-scoped view calls | `tests/test_admin_metrics.py`, `trust-pages.spec.ts` |
| Portal `/members/invite` — a refused file "never left your computer" | The header allowlist runs in the browser on the first line of the file, before any request is made; the upload only happens after it passes. The server repeats the check, so the browser's copy is a privacy measure rather than the guarantee | `portal-live.spec.ts::an eligibility file with a health column is refused without being uploaded` (it watches the network, not the wording) |
| Portal `/members/invite` — "any other column is refused and the file is not imported" | An allowlist over the header, not a denylist of words, and the whole file is rejected rather than the column dropped — a silently ignored column teaches the administrator that sending it was fine | `tests/test_eligibility_import.py::test_a_file_carrying_health_data_imports_nothing_at_all`, `tests/test_eligibility_import.py::test_the_allowlist_stops_columns_no_denylist_would_have` |
| Portal `/members/invite` — "CereBro does not report addresses back to you here" | The import report carries line number, `external_ref` and outcome only. The audit row for an import carries counts | `tests/test_eligibility_import.py::test_the_report_never_contains_an_email_address`, `tests/test_eligibility_import.py::test_the_trail_records_one_action_not_five_hundred` |
| `/account` (sponsored member) — "nothing to pay or cancel here" | Not a hidden button: the branch renders no upgrade CTA and no billing-portal link, because a sponsored member has no Stripe customer for one to open. `sponsored` comes from `services/entitlements.resolve`, which sets it only when an organisation — not a purchase — is paying | `tests/test_entitlements.py::test_me_reports_the_tier_the_server_will_enforce`, `tests/test_entitlements.py::test_a_purchase_is_never_reported_as_sponsored` |
| `/account` (sponsored member) — "They can see that a seat is used; they never see what you write, log or say" | The organisation boundary, restated at the one place the member is told an employer is involved. Sponsorship reads `org_memberships` and nothing else; the org model, service and routes import no wellbeing model at all, so there is no read path to what they write | `tests/test_org.py` (the structural import test), `tests/test_org.py::test_ending_sponsorship_keeps_the_account` |
| `/account` (sponsored member) — "If the sponsorship ends, your account stays — it returns to the free tier" | Entitlement is computed per request and never written to `users.subscription_tier`, so an ended membership stops granting the moment it ends, and the account itself is untouched by the change | `tests/test_entitlements.py::test_the_stored_tier_is_never_written_by_sponsorship`, `tests/test_entitlements.py::test_the_cap_returns_when_the_sponsorship_ends` |
| iOS `PremiumView` / Android `PremiumScreen` (sponsored member) — "There is nothing to pay or cancel here" | The same absence the web account screen makes, on the two surfaces that used to contradict it: iOS renders `sponsoredState`, which has no product cards, no purchase CTA and no Apple manage-subscriptions link; Android renders no price list. Both branch on the server's `sponsored` flag, never on StoreKit or a local guess | `tests/test_entitlements.py::test_me_reports_the_tier_the_server_will_enforce`, `tests/test_entitlements.py::test_a_purchase_is_never_reported_as_sponsored` |
| iOS / Android (sponsored member) — "group totals only, and only for groups large enough that no one person can be picked out of them" | `reporting_threshold` is clamped to `MIN_REPORTING_THRESHOLD` on write, so an administrator cannot lower it to a number that would identify someone; it is a floor, not a default | `tests/test_org.py::test_threshold_cannot_be_set_below_the_floor`, `tests/test_org.py::test_patching_the_threshold_clamps_rather_than_rejecting`, `tests/test_org.py::test_a_group_over_the_threshold_reports_numbers` |
| Android `Session.rememberEntitlement` — the remembered tier is never an unlock | It decides what a screen *says*, not what an account may use: `usage.enforce_quota` and `media.is_entitled` resolve server-side through `services/entitlements`, and the cache is dropped at sign-out so it cannot outlive the account that earned it | `SessionStoreTest::the_entitlement_is_remembered_and_does_not_outlive_the_account`, `tests/test_entitlements.py::test_sponsorship_lifts_the_free_chat_cap`, `tests/test_entitlements.py::test_premium_narration_follows_the_resolved_tier` |

## 5. Deliberately banned phrases

Currently blocked by the gate because they were, or would be, false here:

| Phrase | Why |
| --- | --- |
| "unlimited voice" | Voice is not metered at all — the phrase implied a meter and a benefit that did not exist. Shipped in Android `premium_intro` until 2026-07-30. |
| "richer voice sessions" | The same claim wearing a comparative instead of a quantity, and it outlived the ban above by two weeks — on the **pricing table**, where an unbacked line is a charge for something the buyer never receives. Voice is not tier-gated anywhere; `services/usage.py` meters chat messages only. Removed from the landing 2026-08-15 (audit E3). |
| Pricing "Premium" beside anything the backend does not gate on tier | The backend gates exactly two things — the daily message cap (`services/usage.py`) and premium narration (`services/media.playback_url`). "Daily plans that adapt to your check-ins" sat under Premium while the adaptive plan shipped to every tier (audit E4); a gate implied in a price column is the most expensive kind of false claim. Check `entitlements.PAID_TIERS` before adding a bullet. |
| "it never resets" (of the streak / presence ring) | `metrics.user_streak` forgives one missed day and then does start over — see the capability row above for the true version. |
| "downloaded soundscape" / "available offline" / "offline playback" / "download for offline" | **No client implements downloads** — no `AVAssetDownloadTask`, no ExoPlayer `DownloadService`, and `apps/app/public/sw.js` is push-only. Shipped in iOS `Dummy.offline` and the web library footnote until 2026-07-30. |
| "clinically proven", "fda approved", "cures …", "treats depression" … | This is not a clinical product. False by construction, not merely unproven. |
| "guaranteed to", "100% effective", "risk-free" | No wellness product can promise an outcome. |
| "nothing is ever shared" / "never shared with anyone" | **False.** A Talk message goes to OpenAI or Anthropic (`services/ai.py`); voice goes to Deepgram or ElevenLabs (`services/voice.py`). Shipped as the second sentence on the Welcome screen of Android, iOS *and* the browser client until 2026-07-30 — the first privacy statement a new user ever read, and the strongest one the product cannot keep. The true version is the row above. |

**Note on offline:** the app genuinely *is* local-first in places (curated
fallbacks, on-device journal analysis, `LocalCompanion`, and a safety plan
cached on every client). That is real and can be described — but in those
words, not as "available offline", which reads as downloaded media.
