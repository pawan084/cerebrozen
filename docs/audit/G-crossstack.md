# Audit G — Cross-stack consistency & project hygiene

Date: 2026-08-04. Scope: hand-duplicated contracts (backend ⇄ iOS ⇄ Android ⇄ web app ⇄ landing),
i18n, crisis-number consistency (authority `backend/app/services/crisis.py`), analytics/funnel
instrumentation, claims coverage, tests/CI vs docs, and docs drift. Every point names the files
that prove it. Verified-consistent notes are included where the contract table demanded a check.

## Cross-stack contracts

1. [CONTRACT] The "Assessment taxonomy" contract row points at dead iOS code — ARCHITECTURE.md:278 names `Dummy.motivations` / `Dummy.goalCategories` as the iOS side, but no screen references them anymore (grep: only the definitions at `apps/ios/CereBro/Models/DummyData.swift:124-135` exist); iOS onboarding now uses `StateCheckScreen.states` (6 feeling→motivation/goal pairs) at `apps/ios/CereBro/Features/Onboarding/OnboardingFlow.swift:405-412` (docs/ARCHITECTURE.md, apps/ios/CereBro/Models/DummyData.swift, apps/ios/CereBro/Features/Onboarding/OnboardingFlow.swift)
2. [CONTRACT] Two backend goal-taxonomy members are unreachable from every client — `services/assessment.py` GOALS includes "Practice meditation" and "Calmer mornings", but the 6 feeling options on iOS (`OnboardingFlow.swift:405-412`), Android (`OnboardingScreen.kt:196-201`) and web (`apps/app/lib/onboarding.ts:26-32`) only ever map to the other 6 goals; no client UI can select them, so `normalize_selection` keeps them valid for no caller (backend/app/services/assessment.py:34-42)
3. [CONTRACT] Mood taxonomy has quietly forked between mobile and web — iOS `Dummy.moods` and Android `MOODS` both offer Good/Anxious/Low/Tired (apps/ios/CereBro/Models/DummyData.swift:41-46, apps/android/.../ui/screens/TodayScreen.kt:110-115), while the web app offers Great/Good/Okay/Low/Anxious with no Tired (apps/app/app/(authed)/home/page.tsx:12-18); the server accepts any ≤60-char string (`MoodCreate.mood`, backend/app/schemas/content_data.py:9-14), so nothing pins the set
4. [CONTRACT] Web mood scoring drops "Tired" — both score maps `{Great:5,Good:4,Okay:3,Low:2,Anxious:1}` omit Tired, so a mobile-logged Tired check-in silently scores the fallback 3 ("Okay") in the web home chart and insights average (apps/app/app/(authed)/home/page.tsx:108, apps/app/app/(authed)/insights/page.tsx:14,34); conversely web-only "Great"/"Okay" hit `default:` copy on iOS (`HomeView.swift:317-322`) and have no `MoodOption` on Android
5. [CONTRACT] Android onboarding can post a mood its own client doesn't model — the "Can't stay consistent" state option logs mood "Okay" (apps/android/.../ui/screens/OnboardingScreen.kt:201), but "Okay" is not in Android's `MOODS` (TodayScreen.kt:110-115); iOS/web onboarding post no mood at all (OnboardingFlow.swift `pick()` saves assessment only), so the three funnels leave different first rows in `mood_logs`
6. [CONTRACT] State-tuned journal prompt matching differs in robustness per client — iOS matches case-sensitive exact strings (`case "Anxious"`, apps/ios/CereBro/Features/Journal/JournalViews.swift:23-37), Android trims+lowercases (`tunedPromptFor`, apps/android/.../ui/screens/JournalScreen.kt:90) and web uses broader regexes incl. synonyms `tense|heavy|sad|exhaust` (apps/app/app/(authed)/journal/page.tsx:24-28); the Android test's stated rationale ("the browser client posts 'anxious'", TunedPromptTest.kt:38-40) is factually wrong — web posts capitalized `m.name` (home/page.tsx:96) — so the one client that would actually break on a lowercase mood is iOS
7. [CONTRACT] "Color breathing" paces differently on the two phones — iOS `Preset.color` is 4-2-6 (in/hold/out, apps/ios/CereBro/Features/Tools/ToolsViews.swift:66-70) while Android deliberately gives Color the Box 4-4-4-4 cycle with two holds (`breathePhases`, apps/android/.../ui/screens/Breathe.kt:146-151, pinned by BreathePacingTest.kt:27-28); this is the same class of drift the 2026-07-29 Reset fix corrected, still live for the Color preset (web has no Color surface)
8. [CONTRACT] Breathe pace selection (Classic 4s / Gentle 6s / Slow 8s) exists only on Android — `breathePhases(preset, secondsPerPhase)` scales every preset (Breathe.kt:141-152) but iOS presets are fixed literals (ToolsViews.swift:61-75) and web `SLOW_EXHALE`/`BOX_BREATH` are fixed ms tables (apps/app/components/RitualSteps.tsx:29-40); the ARCHITECTURE breathe-preset row doesn't mention the pace dimension at all (docs/ARCHITECTURE.md:281)
9. [CONTRACT] iOS offline crisis-keyword net covers 9 of the backend's 22 crisis terms — `LocalCompanion.crisisTerms` (apps/ios/CereBro/Features/Talk/LocalCompanion.swift:10-13) omits "killing myself", "ending my life", "take my own life", "taking my own life", "end it all", "ending it all", "suicidal", "wanting to die", "wish i was/were dead", "hurting myself", "harm myself", "harming myself" from `safety._CRISIS_TERMS` (backend/app/services/safety.py:22-29); note "suicidal" is NOT a substring match of "suicide", so an offline "I feel suicidal" gets a plain reflective reply; iOS also mirrors none of `_ELEVATED_TERMS`
10. [CONTRACT] Product-id contract is backend⇄iOS only; Android's paywall is a dead end — all four ids (`com.cerebrozen.premium{,human}.{monthly,annual}`) match between `appstore.py:27-30`, `Products.storekit:36-101` and `SubscriptionManager.swift:17-18` (verified), but Android `PremiumScreen` shows only the two premium prices from string resources, its CTA is `enabled = false`, and there is no Play Billing nor any `subscription_tier` read anywhere in the Android app (apps/android/.../ui/screens/Settings.kt:398-410; grep `subscription_tier` in apps/android = 0 hits)
11. [CONTRACT] Premium+Human is not purchasable outside iOS — web checkout hardcodes `tier: "premium"` (apps/app/app/(authed)/account/page.tsx:188) even though `stripe_billing.price_for` supports `premium_human` and env prices exist (backend/app/services/stripe_billing.py:44-49); Android's paywall omits the tier entirely (Settings.kt:403-406 vs iOS Products.storekit 1499/11999); the tier is renderable on web (`{premium_human: "Premium + Human"}`, apps/app/app/(authed)/layout.tsx:130-134) but unreachable
12. [CONTRACT] Android hardcodes the free-tier quota in marketing copy while the limit is env-tunable — `premium_intro` says "free includes 50 messages a day" as a literal (apps/android/app/src/main/res/values/strings.xml:845) but the number is `settings.free_daily_messages` (default 50, backend/app/core/config.py:47); iOS and the Android free-limit card itself render the server-sent `limit` (`PremiumViews.swift:150`, strings.xml:1477 `%1$d`), so one env change makes only this one string false
13. [CONTRACT] Free-limit 429 contract verified consistent — all three clients match on `detail.code == "free_daily_limit"`, never the status (apps/ios/CereBro/Networking/APIClient.swift:768, apps/android/.../net/Session.kt:155, apps/app/lib/api.ts:150) against `usage.FREE_LIMIT_CODE` (backend/app/services/usage.py:26,72-77)
14. [CONTRACT] Media-catalogue keys verified consistent — Android `MediaCatalog.Keys` mirrors all 18 seeded keys byte-for-byte incl. `game.pad.$index` (apps/android/.../audio/MediaCatalog.kt:27-48 vs backend/app/seed.py:370-402); iOS and web still don't consume the catalog, exactly as the contract row states (docs/ARCHITECTURE.md:305)
15. [CONTRACT] Consent categories verified consistent — the six columns of `models/consent.py:39-44` match web's `Consent` type (apps/app/lib/onboarding.ts:49-56) and the 13-language notice keys on all three clients (see finding 33)
16. [CONTRACT] Mood `intensity` means three different things — web always posts the constant 3 (apps/app/app/(authed)/home/page.tsx:96), Android carries a per-mood default (2/4/4/3, TodayScreen.kt:110-115), iOS exposes a user slider (`MoodCheckinView`, HomeView.swift:305+); server clamps 1-5 (content_data.py:13) so trend math built on intensity would compare unlike quantities
17. [CONTRACT] Sleep-diary `source` enum allows only `manual|healthkit` — `SleepLogCreate.source` pattern (backend/app/schemas/content_data.py:179) has no value for a future Android Health Connect import even though SLEEP_TRACKING.md plans one; today Android/web can only post `manual` (verified harmless, but the enum is the contract to extend)
18. [CONTRACT] Onboarding language list verified consistent (English/Hindi/Hinglish/Punjabi/Tamil) across iOS `Dummy.languages` (DummyData.swift:123), web `LANGUAGES` (apps/app/lib/onboarding.ts:34) and Android `ob_lang_*` options (OnboardingScreen.kt:208-212); server deliberately accepts free text (backend/app/services/language.py:22-27)
19. [CONTRACT] All 8 widget kinds verified handled on iOS and Android, but web `WIDGET_LINKS` omits `dbt_skill` — the only backend kind with no web surface; a `dbt_skill` widget on web renders the "arrives with the mobile apps" footnote (backend/app/services/activities.py:66-75, apps/ios/CereBro/Features/Talk/ChatActivities.swift:9-23, apps/android/.../ui/screens/TalkScreen.kt:148-159, apps/app/app/(authed)/chat/page.tsx:53-62)
20. [CONTRACT] Unknown-widget-kind degradation differs per client — iOS `default:` renders `BreathingView()` so a future kind silently opens a breathing exercise under the wrong card title (ChatActivities.swift:22), while Android renders the card with no Open button (`talk_ios_only`, TalkScreen.kt:1486-1492) and web shows an honest footnote (chat/page.tsx:295-299); iOS should degrade like the other two
21. [CONTRACT] `human_support` suggestion chip navigates only on Android — backend emits it for elevated risk (backend/app/services/activities.py:126); Android opens the HumanSupport screen (TalkScreen.kt:1219) but iOS's `SuggestionChipRail.navActions` omits `human_support` entirely (ChatActivities.swift:197-198; zero grep hits in apps/ios) so the "talk to a person" chip is sent back as chat text, and web special-cases only `crisis` — every other action incl. breathing/grounding is posted as a message (chat/page.tsx:156,334)
22. [CONTRACT] Android's crisis-region picker drifts from the contract's region set — it offers `EU` (a code `crisis.py` doesn't know; falls to DEFAULT) and omits `IE`, which backend/iOS/web all support (apps/android/.../ui/screens/Settings.kt:221-226 vs backend/app/services/crisis.py:12-46, apps/ios/CereBro/Features/Safety/CrisisResources.swift supportedRegions, apps/app/app/(authed)/account/page.tsx:42-51)
23. [CONTRACT] Streak endpoint verified consistent in intent — `GET /users/me/streak` documents itself as the cross-stack mirror of the iOS local rules (backend/app/api/routes/users.py:68-74); goals/habits deliberately expose no streak on any of the three clients (apps/app/app/(authed)/goals/page.tsx:140, backend/app/api/routes/habits.py:10,144)

## i18n

24. [I18N] Hindi is 623 of 1,561 keys (39.9%) behind English — `values-hi/strings.xml` has 938 `<string>` keys vs 1,561 in `values/strings.xml` (computed by script, exact set diff); the biggest missing blocks are `mg_*` mind-games (93), `builder_*` ritual builder (45), `toolkit_*` (36), `oi_*` offline imagery (31), `ombct_*` (27), `talk_*` (26), `winddown_*` (26), `ocg_*` offline crisis grounding (25), `trends_*` (25), `today_*` (23), `imagery_*` (21), `ocbti_*` (21), `tipp_*` (20), `obs_*` (19), plus 34 smaller groups (apps/android/app/src/main/res/values/strings.xml, values-hi/strings.xml)
25. [I18N] Only ~100 of those 623 gaps are the documented deliberate deferrals — the values-hi header (lines 2-26) lists clinical-review holdbacks (TIPP/DBT, CBT reframe, `*_why` provenance, CBT-I cards, disclosure/age-gate, privacy cards), but the ~500-key remainder (mind games, builder, wind-down, imagery, trends, talk, mixer, the whole offline content pack) is undocumented drift from features shipped after the Hindi pass (apps/android/app/src/main/res/values-hi/strings.xml:2-26)
26. [I18N] `MissingTranslation` lint is disabled, so none of finding 24 is caught by tooling — apps/android/app/build.gradle.kts:122 switches the one check off that would have flagged 623 keys (apps/android/app/build.gradle.kts)
27. [I18N] 4 of 13 English plurals are missing from Hindi — `today_insights_count`, `journal_month_count`, `builder_summary`, `journal_found_older` fall back to English silently; the 9 shared plurals have identical quantity sets (apps/android/app/src/main/res/values/strings.xml vs values-hi/strings.xml)
28. [I18N] Zero orphans and zero format-arg mismatches — every values-hi key exists in values, and all 938 shared keys carry identical printf placeholder sets, so there is no `getString`-crash risk (verified by script over both files)
29. [I18N] The only hardcoded user-facing English in Android code is the biometric prompt — `.setTitle("Confirm screen lock")` / `.setSubtitle("Use your phone PIN, password, pattern, or biometrics")` bypass resources (apps/android/app/src/main/java/com/cerebrozen/app/ui/screens/BiometricGate.kt:34-35); everything else that greps as a literal is glyphs/interpolation (GameSession.kt:738, Games.kt:306, TalkScreen.kt:1614)
30. [I18N] Android maps server-sent suggestion chip strings to string resources by English literal match — TalkScreen.kt:264-270 keys off exact English phrases, so a server copy tweak (or a Hindi-directive reply) silently renders the raw server string untranslated (apps/android/.../ui/screens/TalkScreen.kt:264-270)
31. [I18N] iOS has zero localization infrastructure — no `.lproj`, `Localizable.strings`, `.xcstrings`, `NSLocalizedString` or `String(localized:)` anywhere under apps/ios; all copy is hardcoded English across ~30 `Text("...")` files (e.g. apps/ios/CereBro/Features/Safety/SafetyPlanView.swift:62, Features/Onboarding/OnboardingFlow.swift:122, Features/Talk/AIDisclosure.swift:102) — while the product sells Hindi/Punjabi/Tamil *replies* (finding 18), the shell never localizes
32. [I18N] Web apps have no i18n framework — no next-intl/i18next/lingui in any package.json, no locales dir; all JSX copy is hardcoded English (apps/app/app/(authed)/library/page.tsx:68, apps/app/app/crisis/page.tsx:17-24, apps/app/app/(authed)/journal/page.tsx:25-27), the sole exception being the consent-notice module
33. [I18N] The DPDP consent notice is verified in sync across all three clients — same 13 languages in the same order (en, hi, bn, te, mr, ta, ur, gu, kn, ml, or, pa, as) and the same 6 consent keys in `Trust/ConsentNotice.swift:70`, `ui/screens/ConsentNotice.kt:29` (`NOTICE_CODES`), and `apps/app/lib/consentNotice.ts:32-248`; Android's omission of the `recommendOn/Off` card strings is documented in its header
34. [I18N] The stale comment at values-hi:64 says the crisis screen is "deliberately not translated" while the crisis block at values-hi:880-888 is in fact translated — the file's own annotations no longer describe its contents (apps/android/app/src/main/res/values-hi/strings.xml:64,880-888)

## Crisis numbers (authority: backend/app/services/crisis.py)

35. [CRISIS] Backend ⇄ iOS verified in exact agreement — all 7 regions + default match names, numbers and ordering (IN = Tele-MANAS 14416 → 112 → KIRAN 1800-599-0019; default 112 + findahelpline.com), pinned by backend/tests/test_crisis.py and iOS UI tests (CereBroUITests.swift:481-522); iOS adds text lines (741741, Shout 85258, 50808, 0477 13 11 14) as a documented superset (backend/app/services/crisis.py, apps/ios/CereBro/Features/Safety/CrisisResources.swift)
36. [CRISIS] Android's CrisisScreen is not region-aware at all — a static Tele-MANAS/112/KIRAN/findahelpline list renders for every user regardless of stored region, so a US Android user in crisis is shown 112 and 14416, never 911/988 (apps/android/app/src/main/java/com/cerebrozen/app/ui/screens/Extras.kt:2552-2557 vs crisis.py `_REGIONS`)
37. [CRISIS] Android's region picker makes a false claim — `crisisregion_intro` says "Sets which emergency lines the crisis screen shows" (values/strings.xml:818; Hindi mirror values-hi:966) but the picker only PATCHes `region` to the server (Settings.kt:219-243) and the Android crisis screen ignores it (finding 36); only server chat suffixes change
38. [CRISIS] Android's in-chat crisis banner dials India for everyone — `talk_crisis_call` = "Call 14416" wired to `tel:14416` (values/strings.xml:214, TalkScreen.kt:917), and the same hardcoded `tel:14416` sits on the You-tab support door (Screens.kt:170) and Toolkit footer subtitle (Extras.kt:2046); a GB user's crisis banner offers an Indian hotline
39. [CRISIS] The web app's static crisis surfaces are India-only despite a working region picker — `CRISIS_LINES` (apps/app/lib/crisis.ts:14-19) feeds `/crisis`, `/support` and the journal crisis banner; `/support` at least says "These lines are for India", but the journal elevated-risk banner shows 14416/112/KIRAN with no locality note (apps/app/app/(authed)/journal/page.tsx:133-135); only the chat SSE path renders the server's region-correct block (chat/page.tsx:120-224)
40. [CRISIS] Web chat's fallback crisis banner ignores region — when a `crisis` suggestion arrives without a resources block, `setCrisis({})` renders the static India list even for a US/GB-region user (apps/app/app/(authed)/chat/page.tsx:156,221-223)
41. [CRISIS] iOS scatters "Tele-MANAS 14416" in region-independent teaser copy — MicroActivities.swift:43, JournalViews.swift:90, GamesHub.swift:54, ProfileView.swift:83, SafetyViews.swift:27 all say "Tele-MANAS 14416 · real people, 24/7" for every region; the screens they open adapt, the row copy doesn't (apps/ios/CereBro/Features/...)
42. [CRISIS] HumanSupport is India-hardcoded on all three clients (identical to each other: Tele-MANAS 14416, iCall 9152987821, findahelpline.com/in) — good cross-client parity, but only Android/web copy hints at the India scope; iOS gives no qualifier (apps/ios/CereBro/Features/Safety/SafetyViews.swift:14-22, apps/android/.../Settings.kt:429-433, apps/app/app/(authed)/account/page.tsx:383-401)
43. [CRISIS] Every literal number in the repo is canonical and dialable — no stale or wrong hotline numbers found anywhere; iOS strips to digits before `tel://` (CrisisResources.swift:120-127), web strips non-digits (lib/crisis.ts:33-35), Android uses ACTION_DIAL which tolerates dashes (Extras.kt:2461-2473); values-hi numbers all match values (14416 at values-hi:138,896,924,930,958)
44. [CRISIS] Escalation and onboarding safety copy are region-safe by construction — trusted-contact/ops messages say "contact local emergency services" with no hardcoded number (backend/app/services/escalation.py:49-53), as do `ob_danger_line` (strings.xml:552) and `ocg_emergency_disclaimer` (strings.xml:1502)
45. [CRISIS] Cosmetic findahelpline URL variance — backend default `findahelpline.com`, human-support surfaces `…/in`, Android CrisisScreen bare `findahelpline.com` (crisis.py:51, SafetyViews.swift:20, Extras.kt:2556); all resolve, one canonical form would be tidier

## Analytics

46. [ANALYTICS] Event vocabulary verified closed — every event any client emits (`onboarding_step`, `onboarding_done`, `paywall_view`, `paywall_cta`) is in `ALLOWED_EVENTS`, and unknown names are silently skipped with a reduced `accepted` count, not a 422 (backend/app/api/routes/events.py:22-27,49-50)
47. [ANALYTICS] Android never fires `paywall_cta` — `PremiumScreen` tracks only `paywall_view` (apps/android/.../ui/screens/Settings.kt:400; zero `paywall_cta` hits in apps/android/app/src/main), so the admin `paywall_taps` metric (backend/app/services/metrics.py:215) is permanently dark for Android while iOS (PremiumViews.swift:22,82) and web (account/page.tsx:183) light it
48. [ANALYTICS] `source: "web"` is a dead enum member — the landing (`apps/web`) sends no events at all and the browser client sends `source: "app"` (apps/app/lib/analytics.ts:113), so the `web` value in the source pattern (events.py:39) is never used
49. [ANALYTICS] `paywall_cta.step` mixes vocabularies — iOS sends the StoreKit product id, web sends the literal `"premium"`, Android sends nothing (apps/ios/.../PremiumViews.swift:82, apps/app/app/(authed)/account/page.tsx:183); the backend comment says step = product id (events.py:26), so per-product CTA aggregation is already unreliable
50. [ANALYTICS] Consent gating is consistent in mechanism (unlock-at-consent + device-local opt-out; `/events` is deliberately unauthenticated) and no client fires pre-consent — web `cerebro_analytics_unlocked`/`cerebro_usage_stats` (analytics.ts:27-30,102-104), iOS `analytics_unlocked`/`usageStatsOn` (Analytics.swift:15-48), Android `analytics_unlocked`/`usage_stats_on` (net/Analytics.kt:18-42) — but note there is NO analytics column in `models/consent.py`; the opt-out is per-device only and does not sync with the account on any stack
51. [ANALYTICS] The funnel bars mean different populations per client — pre-unlock steps (`welcome`…`first_reset`) are dropped everywhere (deliberate), but `consent` is counted from web (explicit re-fire post-unlock, onboarding/page.tsx:95), only-if-signed-up from iOS (fired on entry, OnboardingFlow.swift:39-45), and never from Android (fired on entry pre-unlock, no re-fire, OnboardingScreen.kt:310,454); `signup` is effectively Android-only because iOS/web fire it on entry pre-unlock
52. [ANALYTICS] `onboarding_done` is not one event — iOS/web fire it on completing the notifications step with account optional (OnboardingFlow.swift:47, onboarding/page.tsx:65) but Android fires it only inside `onAccountCreated` (OnboardingScreen.kt:506), so Android's "done" means "account created" and cross-source completion rates are not comparable
53. [ANALYTICS] Android's chronological step order differs — Android runs …Reset→Consent→Notify→SignUp while iOS/web run …first_reset→signup→consent→notifications (OnboardingScreen.kt:150 `OStep` vs OnboardingFlow.swift:13-16, lib/onboarding.ts:12-15); names all map, but the admin funnel chart is non-monotonic across sources and the unlock asymmetries in finding 51 follow from this
54. [ANALYTICS] `age_gate` and `first_plan` are dead vocabulary — pinned in `metrics.ONBOARDING_STEPS` (metrics.py:187-190, test_events.py:86-98) but fired by no client since the age-gate merge and first-plan deletion; two permanently-zero bars in the admin funnel
55. [ANALYTICS] Android's `funnelStepName` has the exact silent-drop hole the contract warns about — `else -> step.lowercase()` (net/Analytics.kt:59) means a future `OStep` member without an explicit mapping emits its Kotlin enum name, which `metrics.py` drops without error; iOS/web index fixed arrays and cannot drift this way
56. [ANALYTICS] Beyond the funnel and paywall, all product screens are dark on every client symmetrically — chat, journal, sleep, toolkit fire no events anywhere by design (engagement comes from first-party tables via `metrics.overview()`); verified this is parity, not a one-client gap (backend/app/services/metrics.py)

## Claims (docs/CLAIMS_MAP.md + scripts/check-claims.mjs)

57. [CLAIM] The landing's trusted-contact promise has no CLAIMS_MAP row — "…you can nominate a trusted contact to notify" (apps/web/app/page.tsx:42); the mechanism exists (backend/alembic/versions/7a6e3c9f5b24_trusted_contact_and_escalation.py) but notification silently no-ops without SMTP/Twilio keys, and no banned phrase would catch a regression (docs/CLAIMS_MAP.md, scripts/check-claims.mjs)
58. [CLAIM] "core tools … work without a connection" (apps/web/app/page.tsx:67) is an unrowed paraphrase of the banned "available offline" family — plausibly true today (bundled loops in apps/android/app/src/main/res/raw, apps/ios/CereBro/Resources/Sounds, synthesis in SoundscapePlayer.swift) but the phrase gate would not catch drift back to the false version
59. [CLAIM] Three uncovered landing capability claims — "lock it behind Face ID if you like" (page.tsx:28; LAContext exists at apps/ios/CereBro/Features/Journal/JournalViews.swift:111-143 but nothing pins it), "Agentic daily plan that adapts to your mood + journal" and "Timed check-ins when patterns shift" (page.tsx:33-35; the patterns row covers thresholds only) — none has a CLAIMS_MAP row or gate phrase
60. [CLAIM] "₹0 / Forever" + "free forever" (apps/web/app/page.tsx:47,69) is a forever-pricing promise with no row and no test; nothing stops a future quota change from falsifying it silently
61. [CLAIM] Third-party-behaviour absolutes are unrowed — providers "do not use it to train their models" (apps/web/app/privacy/page.tsx:82-84), subprocessors "never your recordings" / "the stores never see your wellness data" / "does not use your content to train its models" (apps/web/app/subprocessors/page.tsx:28,33,63); claims about other companies' conduct with no mechanism or review cadence named
62. [CLAIM] Unrowed ops promises on the privacy page — "Data is encrypted in transit, access is restricted" (privacy/page.tsx:98; TLS lives in deploy/Caddyfile outside any test) and "Operational logs … rotated automatically" (privacy/page.tsx:111; no named rotation mechanism anywhere in the repo)
63. [CLAIM] "Audio storage is off by default and only retained if you explicitly enable it" (privacy/page.tsx:63,108) rides the generic all-consent-defaults-off row; no row or test names `voice_storage` specifically (backend/tests/test_consent.py)
64. [CLAIM] The security page claims CLAIMS_MAP discipline and then skips it — its own comment (apps/web/app/security/page.tsx:12-13) says the rule applies, yet its seven mechanism claims (TLS auto-renew, bcrypt-only, rate-limited sign-in, rotating refresh tokens, CSP nonces, DB not internet-exposed + boot guard, server-side consent enforcement, lines 45-51) and the "acknowledge within 3 business days" SLA (line 62) have zero rows
65. [CLAIM] The crisis flow states "KIRAN mental-health helpline — 24/7 support in 13 languages" (apps/app/app/crisis/page.tsx:13) — KIRAN was widely reported merged into Tele-MANAS (~2024), making this a doubtable third-party liveness claim in the worst possible place to be wrong; the "Crisis-aware" row covers ordering, not helpline liveness (docs/CLAIMS_MAP.md)
66. [CLAIM] Evidence superlatives render to users with no row even at class level — "the best-evidenced treatment for insomnia (Lancet Digital Health, 2025)" (apps/app/app/(authed)/sleep/ritual/page.tsx:119, games/ritual/page.tsx:425), "the approach with the strongest evidence among app-delivered tools (Linardon et al. 2024)" (apps/ios/.../ToolsViews.swift:189, apps/android strings.xml:915), Pennebaker 1997 (three clients)
67. [CLAIM] "Built with published clinical research; a formal clinical advisory process is on our roadmap" ships on all three clients (apps/app/app/(authed)/account/page.tsx:495, apps/android strings.xml:878, apps/ios/CereBro/Features/Trust/PrivacyViews.swift:111) — the most audit-worthy uncovered "clinical"-adjacent phrase in the product; no row, no gate phrase
68. [CLAIM] "Programs are evidence-informed — built on CBT and sleep-science techniques" (apps/app/app/(authed)/programs/page.tsx:194, apps/ios/.../HomeView.swift:479, strings.xml:772) — unrowed; the check-claims gate bans other phrasings but not this one
69. [CLAIM] Two Android capability claims are unrowed — "Delivered on-device via a scheduled alarm — no server or FCM required" (strings.xml:863) and the "OFFLINE SLEEP EDUCATION" eyebrow (strings.xml:1522), both true today but in the banned-family neighbourhood with nothing pinning them
70. [CLAIM] iOS premium copy "The full sleep library, richer voice sessions, and deeper personalization" (apps/ios/CereBro/Features/Premium/PremiumViews.swift:13) has no row, and the store-facing marketing descriptions inside `Products.storekit` (lines 31,56,76,96) are invisible to check-claims.mjs entirely (its iOS regex is `\.swift$`)
71. [CLAIM] check-claims.mjs has three whole blind surfaces holding user-facing copy — `apps/app/lib/` (consentNotice.ts, crisis.ts incl. 24/7 claims, onboarding.ts) is outside the scanned globs; Android Kotlin composables with hardcoded copy (e.g. ConsentNotice.kt:47) are unscanned (only res/**/strings.xml); and values-hi/strings.xml is scanned but the banned list is English-only, so a Hindi rendering of any banned phrase passes (scripts/check-claims.mjs)
72. [CLAIM] check-claims.mjs path handling is broken on Windows — line 150 strips the repo root with a forward-slash join that never matches backslash paths, so error output prints absolute paths; and the comment exemption (line 148) misses `/* */` continuation lines that don't start with `*` (scripts/check-claims.mjs:148-150)
73. [CLAIM] Verified: the gate currently passes ("No unbacked claims across 112 user-facing files"), no previously-banned phrase has reappeared in user-facing copy, and the refunds-page export/delete promise correctly matches its CLAIMS_MAP row (apps/web/app/refunds/page.tsx:41)

## Tests / CI (docs/TECHNICAL.md vs .github/workflows + e2e)

74. [TEST] TECHNICAL.md still describes the Android CI job as a non-blocking scaffold — "scaffold only, non-blocking in CI" (docs/TECHNICAL.md:11) and "`android` (`continue-on-error`)" (:107) — but ci.yml:186-214 has no `continue-on-error` and its in-file comment documents the deliberate 2026-07-31 flip to blocking after a broken commit landed on main (`.github/workflows/ci.yml`)
75. [TEST] The Android job's real gates are undocumented and the coverage number is wrong — CI runs `:app:check :app:assembleDebug :app:lintVitalRelease` (ci.yml:214: unit tests + full lint + JaCoCo gate + release vital lint), none of which TECHNICAL.md describes, and TECHNICAL.md:69 says ">=95%" while the code gates at 96% (apps/android/app/build.gradle.kts:245,286)
76. [TEST] TECHNICAL.md:69 claims `:app:check` is "currently red (untranslated Hindi strings, testReleaseUnitTest)" — if true every CI run would fail; the job is blocking and green and values-hi exists (apps/android/app/src/main/res/values-hi/strings.xml)
77. [TEST] Two CI gates exist that no doc mentions — the Alembic single-head + upgrade-from-scratch migrations gate (ci.yml:48-64) and the web job's `sync-tokens.mjs --check` / `check-csp-sync.mjs` steps (ci.yml:118,132); TECHNICAL.md:104-110 lists only pytest/coverage, check-claims and check-prices
78. [TEST] The e2e suite outgrew its own name and description — TECHNICAL.md:68 says "Web+Admin e2e … landing, waitlist, admin CRUD" and the CI job is named "Web + Admin (Playwright e2e)" (ci.yml:82-83), but the suite runs 25 tests across 5 specs including the full authed web-app journey, theme, and trust pages, and docker-compose.e2e.yml:50-56 builds the `app` service
79. [TEST] The iOS CI comment is stale — ci.yml:154-156 says "the other 8 run fully" but apps/ios/CereBroUITests now holds 22 `func test…` cases; the job does genuinely run `xcodebuild test` (not build-only), verified (ci.yml:165-177)
80. [TEST] TECHNICAL.md's backend test counts are internally inconsistent and both wrong — ":66 ~445 async tests" vs ":72 the same 330 tests" vs 501 actual test functions across 62 files in backend/tests
81. [TEST] Four shipped web-app surfaces have zero e2e coverage — `/goals` and `/safety-plan` are never visited by any spec (the CLAIMS_MAP safety-plan offline row cites no web e2e, so the localStorage-mirror behaviour is untested end-to-end), `/support` is only asserted as a visible link (app.spec.ts:184-186), and the dedicated `/signup` page never loads (all account creation uses the /signin tab) (e2e/tests/app.spec.ts, apps/app/app/(authed)/goals/page.tsx, safety-plan/page.tsx, support/page.tsx, signup/page.tsx)
82. [TEST] Verified: backend feature coverage is complete — all 23 router modules map to at least one test file (goals/habits→test_habits.py, recommendations, safety_plan, memory, media catalog, programs ×2, stripe ×3) plus a `test_route_coverage.py` meta-test; and the backend coverage gate (95) matches across ci.yml:78, TECHNICAL.md:66 and CLAUDE.md

## Docs drift (PRD / ARCHITECTURE / TECHNICAL / WEB_APP_PLAN vs code)

83. [DOCS] PRD's ‡ caveats point at a branch that no longer exists — PRD.md:8-13,297-298 say ‡-marked work lives unmerged on `fix/ui-worldclass-103`; the branch is gone and its commits are on main (e.g. 6ce9954), so every ‡ reads as false doubt (docs/PRD.md)
84. [DOCS] PRD.md:80 says Oracle confirmations are "ephemeral — … never persisted, no audit table, no admin review surface" — contradicted by `agent_actions` (backend/app/models/agent_action.py, services/oracle_audit.py), `GET /oracle/actions` (routes/oracle.py) and the admin Oracle tab; the same doc's checklist #13 (line 199) marks this DONE
85. [DOCS] PRD.md:110 says the free-tier 429 is "Server side only — no client renders it" — iOS presents `FreeLimitView` (apps/ios/CereBro/Features/Premium/PremiumViews.swift via TalkView), Android renders the Talk card, web the chat card; the doc's own Talk row (line 87) says so
86. [DOCS] "FCM is 100% absent" (PRD.md:139), "no remote push" (PRD.md:145) and "Android has no Firebase" (ARCHITECTURE.md:303) are all false — backend/app/services/fcm.py + `fcm_credentials_path`/`fcm_project_id` (core/config.py:104-105), `firebase-messaging` in apps/android/app/build.gradle.kts:332-333, and CereBroMessagingService.kt registering via `/users/me/devices`
87. [DOCS] Route/module counts are stale across the doc set — Android "~37 routes" (ARCHITECTURE.md:41) and "~40 routes" (CLAUDE.md) vs 57 `composable(` destinations (apps/android/.../ui/CereBroApp.kt); "services/ (22 modules)" (ARCHITECTURE.md:73) vs 31 files in backend/app/services
88. [DOCS] ARCHITECTURE's e2e description is doubly stale — ":48 e2e/ Playwright tests (web + admin)" and ":381 runs 7 tests" vs 25 tests across 5 specs covering three frontends (e2e/tests, docker-compose.e2e.yml:50)
89. [DOCS] ARCHITECTURE's routes summary table (:81-101) omits shipped route groups `/goals`+`/habits`, `/recommendations`, `/safety-plan` even though the same doc's contracts table (:298-300) describes all three (backend/app/api/routes/habits.py, recommendations.py, safety_plan.py)
90. [DOCS] The iOS section is stale on three counts — ":202 dark-only" (Dawn/Night dual theme shipped, apps/ios/CereBro/DesignSystem/Theme.swift:58-127), ":202 ~9.2k LOC" (actual ~13.4k Swift lines), ":227 ~18 screenshot tests" (22 test funcs in CereBroUITests)
91. [DOCS] The VoiceOver contract row says "Android's equivalent is not yet wired" (ARCHITECTURE.md:295) — Android uses Compose live regions: `LiveRegionMode.Polite` at apps/android/.../ui/screens/TalkScreen.kt:1586 and Extras.kt:637,674
92. [DOCS] The push-token contract row contradicts the routes table in the same file — :303 names `users.push_token` as the mechanism while :85 says `/users/me/devices` replaces that column; both routes exist (backend/app/api/routes/users.py:479) and the row needs reconciling
93. [DOCS] Both "Planned (not built)" blocks describe shipped work — ":358-367 Still planned: honest-local iOS insights" (shipped: apps/ios/CereBro/Features/Insights/InsightsViews.swift:18,49-51,103; PRD checklist #7 marks DONE 2026-07-30) and ":368-375 Still planned: chat, plans, insights, content pages, account/consent/export/delete, streaks endpoint, admin analytics/nudges, Stripe web billing, Web Push" — all exist (apps/app/app/(authed)/{chat,plan,insights,library,account}, users.py:68, routes/admin.py, routes/billing.py, services/webpush.py + apps/app/lib/push.ts); only the Apple Services ID remains open
94. [DOCS] ARCHITECTURE's infra section misdescribes today's CI and compose — ":387-389 `android` (non-blocking)" (blocking since 2026-07-31, see 74), the `web` gate job and migrations gate are omitted, ":379 docker-compose.yml — db/api/web/admin" omits the `app` service on 3002 that the compose file defines, and the :8-27 system diagram draws neither the Android lead client nor apps/app (docker-compose.yml:59-64)
95. [DOCS] TECHNICAL.md's env table (:31-53) lists `APNS_*`/`SMTP_*`/`TWILIO_*`/VAPID but not `FCM_CREDENTIALS_PATH`/`FCM_PROJECT_ID` — the only delivery channel absent from the table (backend/app/core/config.py:104-105)
96. [DOCS] WEB_APP_PLAN.md still fences off shipped features — ":38-41 Out of v1: games … payments" (both shipped: apps/app/app/(authed)/games tree, Stripe checkout on account page), ":50 Streaks have no backend endpoint today" (users.py:68), ":52 No Web Push in v1" (shipped, see 93), ":136 Stripe billing unticked" (live + hardened), ":96-115 Admin plane v2 Planned" (items 1-5 shipped per the doc's own §7)
97. [DOCS] Verified still true (checked, deliberately not flagged): "no client renders `GET /oracle/actions` yet", "iOS still reads `today_guide` only", "iOS + web do not consume the media catalogue yet", the iOS unit-test target's absence from project.pbxproj, and coach-booking's concept status (docs/ARCHITECTURE.md:293,302,305)
