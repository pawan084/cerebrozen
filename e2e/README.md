# e2e — End-to-End Suite

Not built yet (Phase 4, see `docs/TODO.md`). Playwright against the composed
Docker stack (`ref/Zen/e2e` pattern): full employee journey, HR aggregates
with cohort floors, ops workbook flow, cross-tenant denial.
